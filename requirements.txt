# Version floors below, not exact pins, so this installs on any Python/OS. The results reported
# in the thesis were produced with exactly these versions: matplotlib==3.11.0, networkx==3.6.1,
# openpyxl==3.1.5, pandas==2.2.3, PyYAML==6.0.3, flake8==7.3.0. If a re-run ever disagrees with
# the reported numbers, pin to these exact versions first to rule out a library-version cause.

# Runtime dependencies
matplotlib>=3.11.0
networkx>=3.6.1
openpyxl>=3.1.5
pandas>=2.2.3
PyYAML>=6.0.3

# Development tooling
flake8>=7.3.0
